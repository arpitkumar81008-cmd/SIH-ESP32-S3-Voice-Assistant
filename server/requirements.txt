# FastAPI Server & Ingestion Engine
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
websockets>=11.0
pyserial>=3.5
faster-whisper>=0.10.0
silero-vad>=0.1.0
torch>=2.0.0
numpy>=1.23.0,<2.0.0
soundfile>=0.12.0

# Optional: Offline TinyML KWS Verification & Feature Extraction
# On Linux: pip install tflite-runtime
# On Windows / Mac: pip install tensorflow
tflite-runtime>=2.14.0; sys_platform == 'linux'
scipy>=1.10.0
